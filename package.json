{
  "name": "jaikisan",
  "version": "1.0.0",
  "description": "Problem Statement:\r Create two API’s which can perform the specified CRUD operations.The project structure should\r have models, middlewares, controllers and services. Write MongoDB queries to fetch, update,\r add or delete data from the specified collections. You can assume that the collections already\r exist in the database and just define the project structure.\r Customer API\r 1. Get all customers List with status ACTIVE [GET]\r 2. Delete customer. [DELETE]\r 3. Create new customer [POST]\r Card API\r 1. Get all Card List[GET]\r 2. Create new card [POST]",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.2",
    "nodemon": "^2.0.21"
  }
}
